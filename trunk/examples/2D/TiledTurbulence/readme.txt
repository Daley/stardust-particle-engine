The BitmapField object calculates its vector field based on an existing BitmapData object.
In this example a 100X100 perlin noise bitmap is used.
The BitmapField.tile property is set to true, so the velocity field corresponds to an infinitely tiled perlin noise image with 100X100 tile size.